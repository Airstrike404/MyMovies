@using System.Web.Optimization
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>@ViewBag.Title MYMovies разработка</title>
    <meta name="viewport" content="width=device-width" />
    <link rel="stylesheet" href="/Content/datatable-bootstrap.css">

    @Styles.Render("~/Content/bootstrap")
    @Styles.Render("~/Content/css")

    @RenderSection("css", false)

    @Scripts.Render("~/bundles/modernizr")

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/jqueryval")
    @Scripts.Render("~/bundles/jqueryui")
    @Scripts.Render("~/bundles/bootstrap")
    @Scripts.Render("~/bundles/bootstrapcontrols")

    @RenderSection("headScripts", false)
</head>
<body>
    <header>
        <div class="navbar navbar-inverse">
            <div class="container">
                <div class="navbar-header" style="margin-top: 10px">
                    <button class="navbar-toggle" type="button" data-toggle="collapse" data-target=".bs-navbar-collapse">
                        <span class="sr-only">Toggle navigation</span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>
                    <a href="#" class="navbar-brand">MyMovies</a>
                </div>
                <div class="navbar-collapse collapse" style="margin-top: 10px">
                    <ul class="nav navbar-nav">
                        <li class="dropdown">
                            
                        </li>
                    </ul>
                    <form class="navbar-form navbar-left">
                        <input type="text" class="form-control col-lg-8 search" placeholder="Поиск">
                    </form>
                    <ul class="nav navbar-nav navbar-right">
                        @Html.Partial("_LoginPartial")
                    </ul>
                </div>
            </div>
        </div>
    </header>

    @RenderSection("featured", required: false)
    @RenderSection("specificScripts", required: false)
    <div id="body">
        @RenderSection("scripts", false)
        @RenderSection("body", false)
    </div>

    <footer>
        <div class="content-wrapper">
            <div class="float-left">
                <p>
                    &copy; @DateTime.Now.Year - MyMovies разработка
                </p>
            </div>
        </div>
    </footer>

    @Scripts.Render("~/bundles/bootbox")
</body>
</html>
